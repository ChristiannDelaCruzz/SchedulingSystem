# SchedulePro — Smart Class Scheduling System

A hardcoded, runnable React + TypeScript demo of an **Automated Academic Scheduling System** for colleges and universities. No Supabase. No backend. All data lives in `src/mocks/` and edits persist in your browser's `localStorage`.

---

## 🚀 Quick Start

```bash
npm install
npm run dev